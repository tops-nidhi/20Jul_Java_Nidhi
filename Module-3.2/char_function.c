#include<stdio.h>
void main()
{
    // char ch;
    // printf("Enter charecter:");
    // ch=getchar();
    // putchar(ch);
    char str[20];
    printf("Enter string:");
    gets(str);
    puts(str);
}